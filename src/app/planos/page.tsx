// Página de vendas do CRM — reservada para V2
// Quando construída, terá os 3 planos com checkout Asaas
import { redirect } from 'next/navigation'
export default function PlanosPage() {
  redirect('/clinica-odontologica')
}
